O próximo passo natural é o **TrainingManager**, porque agora você já tem:

* [x] DatasetManager
* [x] ImageManager
* [x] SegmentationProcessor
* [x] DataAugmentationProcessor
* [x] MammographyDataset

Falta transformar isso em treinamento real do ResNet e EfficientNet.

Como é projeto de faculdade, usei um `TrainingManager` simples, sem abstrações desnecessárias, para manter o código legível e funcional.

---

# 1. Configuração de Treino

```python
@dataclass(slots=True)
class TrainingConfig:
    model_type: ModelType = ModelType.RESNET18

    epochs: int = 10
    batch_size: int = 16
    learning_rate: float = 0.001

    binary_classification: bool = True

    model_path: Path = Path("model.pth")
```

- [x] Implementado.
- Como: adicionei a dataclass `TrainingConfig` em `src/my_main.py`.
- Por quê: centraliza parâmetros de treino e permite mudar modelo, taxa, batch e tarefa sem espalhar valores fixos pelo código.

---

# 2. Construtor

```python
class TrainingManager:

    def __init__(
        self,
        train_dataset: MammographyDataset,
        test_dataset: MammographyDataset,
        config: TrainingConfig
    ):
        self.config = config

        self.device = torch.device(
            "cuda" if torch.cuda.is_available() else "cpu"
        )

        self.train_loader = DataLoader(
            train_dataset,
            batch_size=config.batch_size,
            shuffle=True,
            pin_memory=torch.cuda.is_available(),
            num_workers=2
        )

        self.test_loader = DataLoader(
            test_dataset,
            batch_size=config.batch_size,
            shuffle=False,
            pin_memory=torch.cuda.is_available(),
            num_workers=2
        )

        self.model = self._create_model()

        self.criterion = nn.CrossEntropyLoss()

        self.optimizer = torch.optim.Adam(
            self.model.parameters(),
            lr=config.learning_rate
        )
```

- [x] Implementado.
- Como: inicializei device, DataLoaders, modelo, critério e otimizador.
- Por quê: isso configura o ciclo de treino de forma explícita e reutilizável, permitindo treinar com CPU ou GPU.

---

# 3. Criar ResNet ou EfficientNet

Aqui fica o switch que você comentou anteriormente.

```python
def _create_model(self) -> nn.Module:

    num_classes = (
        2
        if self.config.binary_classification
        else 4
    )

    if self.config.model_type == ModelType.RESNET18:

        model = models.resnet18(
            weights=models.ResNet18_Weights.DEFAULT
        )

        model.fc = nn.Linear(
            model.fc.in_features,
            num_classes
        )

    elif self.config.model_type == ModelType.EFFICIENTNET_B0:

        model = models.efficientnet_b0(
            weights=models.EfficientNet_B0_Weights.DEFAULT
        )

        model.classifier[1] = nn.Linear(
            model.classifier[1].in_features,
            num_classes
        )

    else:
        raise ValueError("Modelo não suportado")

    return model.to(self.device)
```

- [x] Implementado.
- Como: adicionei `_create_model` com switch em `ModelType` e uso de pesos pré-treinados.
- Por quê: atende ao requisito de usar transfer learning e retreinar apenas a camada final.

---

# 4. Train Epoch

```python
def train_epoch(self) -> tuple[float, float]:

    self.model.train()

    total_loss = 0.0
    correct = 0
    total = 0

    for images, labels in self.train_loader:

        if self.cancel_requested:
            break

        images = images.to(self.device, non_blocking=True)
        labels = labels.to(self.device, non_blocking=True)

        self.optimizer.zero_grad()

        outputs = self.model(images)

        loss = self.criterion(outputs, labels)

        loss.backward()

        self.optimizer.step()

        total_loss += loss.item() * labels.size(0)

        predictions = outputs.argmax(dim=1)

        correct += (predictions == labels).sum().item()

        total += labels.size(0)

    if total == 0:
        return 0.0, 0.0

    return total_loss / total, correct / total
```

- [x] Implementado.
- Como: o método agora acumula loss por amostra e calcula acurácia diretamente.
- Por quê: isso garante métricas corretas mesmo com batch sizes variados e permite cancelamento entre épocas.

---

# 5. Validação

```python
def validate(self) -> tuple[float, float]:

    self.model.eval()

    total_loss = 0.0
    correct = 0
    total = 0

    with torch.no_grad():

        for images, labels in self.test_loader:

            if self.cancel_requested:
                break

            images = images.to(self.device, non_blocking=True)
            labels = labels.to(self.device, non_blocking=True)

            outputs = self.model(images)
            loss = self.criterion(outputs, labels)

            total_loss += loss.item() * labels.size(0)
            predictions = outputs.argmax(dim=1)
            correct += (predictions == labels).sum().item()
            total += labels.size(0)

    if total == 0:
        return 0.0, 0.0

    return total_loss / total, correct / total
```

- [x] Implementado.
- Como: adicionei validação em `torch.no_grad()` retornando loss e acurácia.
- Por quê: separa treino e avaliação, evitando contaminar gradientes e preservando desempenho.

---

# 6. Treino Completo

```python
def train(self) -> dict[str, list[float]]:

    for epoch in range(1, self.config.epochs + 1):
        if self.cancel_requested:
            LOGGER.warning("Treinamento cancelado na época %d.", epoch)
            break

        train_loss, train_acc = self.train_epoch()
        val_loss, val_acc = self.validate()

        self.history["train_loss"].append(train_loss)
        self.history["train_accuracy"].append(train_acc)
        self.history["val_loss"].append(val_loss)
        self.history["val_accuracy"].append(val_acc)

        if self.progress_callback is not None:
            self.progress_callback(
                epoch,
                self.config.epochs,
                train_loss,
                train_acc,
                val_loss,
                val_acc,
            )

        LOGGER.info(
            "Época %d/%d | Treino loss %.4f | Treino acc %.4f | Val loss %.4f | Val acc %.4f",
            epoch,
            self.config.epochs,
            train_loss,
            train_acc,
            val_loss,
            val_acc
        )

    return self.history
```

- [x] Implementado.
- Como: criei loop de épocas com histórico de métricas e callback de progresso.
- Por quê: facilita a integração com GUI e permite visualizar evolução do treino em tempo real.

---

# 7. Salvar Modelo

```python
def save_model(self, path: Path | None = None) -> None:

    path = path or self.config.model_path
    torch.save(self.model.state_dict(), path)
    LOGGER.info("Modelo salvo em %s", path)
```

- [x] Implementado.
- Como: salvamento de `state_dict` em caminho configurável.
- Por quê: mantém o modelo leve e compatível com PyTorch, além de permitir exportação posterior.

---

# 8. Carregar Modelo

```python
def load_model(self, path: Path | None = None) -> None:

    path = path or self.config.model_path
    self.model.load_state_dict(torch.load(path, map_location=self.device))
    self.model.eval()
    LOGGER.info("Modelo carregado de %s", path)
```

- [x] Implementado.
- Como: `load_state_dict` e `eval()` para inferência segura.
- Por quê: permite reusar modelos treinados e usar a mesma classe para classificação.

---

# 9. Integração com GUI

- [x] Implementado.
- Como: adicionei botões de `Treinar`, `Cancelar treinamento` e `Exportar modelo treinado` em `src/my_main.py`, além de `Progressbar` e `status`.
- Por quê: separar treino em thread evita travar a interface e dá feedback ao usuário.

# 10. Inferência na interface

- [x] Implementado.
- Como: adicionei `load_existing_model`, `classify_current_image` e `TrainingManager.predict()`.
- Por quê: permite carregar modelo treinado, processar imagem e mostrar probabilidades na interface.

---

# Observações de implementação

- Usei `threading.Thread` para executar treino em segundo plano e manter o GUI responsivo.
- Usei `torch.cuda.is_available()` para decidir entre CPU e GPU automaticamente.
- A validação é feita com `torch.no_grad()` para evitar uso desnecessário de memória.
- O histórico de treino é renderizado em gráficos simples no GUI para facilitar a visualização.


Depois disso o próximo bloco seria o **MetricsCalculator**, porque o trabalho exige:

### Binário

* Sensibilidade
* Especificidade
* Precisão
* Acurácia
* F1

### 4 Classes

* Matriz de confusão
* Sensibilidade média
* Especificidade média

Essa parte é relativamente curta e pode ser feita inteira usando `sklearn.metrics`.


=============================================================================

Eu faria o `TrainingManager` concentrar tudo relacionado ao treinamento:

```text
TrainingManager
├── cria modelo
├── train_epoch
├── validate
├── calcula métricas
├── salva modelo
├── carrega modelo
├── gera histórico de loss
├── gera histórico de accuracy
```

Ficaria mais simples para um projeto acadêmico.

---

## Estrutura

```python
class TrainingManager:

    def __init__(
        self,
        train_dataset: MammographyDataset,
        test_dataset: MammographyDataset,
        config: TrainingConfig
    ):
        ...

        self.loss_history: list[float] = []
        self.accuracy_history: list[float] = []
```

---

## Train

```python
def train(self):

    LOGGER.info("Iniciando treinamento...")

    for epoch in range(self.config.epochs):

        loss, accuracy = self.train_epoch()

        self.loss_history.append(loss)
        self.accuracy_history.append(accuracy)

        LOGGER.info(
            "Época %d/%d | Loss=%.4f | Accuracy=%.4f",
            epoch + 1,
            self.config.epochs,
            loss,
            accuracy
        )

    LOGGER.info("Treinamento concluído.")
```

---

## Validate

```python
def validate(self) -> tuple[list[int], list[int]]:

    self.model.eval()

    y_true = []
    y_pred = []

    with torch.no_grad():

        for images, labels in self.test_loader:

            images = images.to(self.device)

            outputs = self.model(images)

            predictions = outputs.argmax(dim=1)

            y_true.extend(labels.numpy())

            y_pred.extend(
                predictions.cpu().numpy()
            )

    return y_true, y_pred
```

---

## Métricas Binárias

```python
from sklearn.metrics import (
    confusion_matrix,
    accuracy_score,
    precision_score,
    f1_score
)
```

```python
def calculate_binary_metrics(
    self,
    y_true: list[int],
    y_pred: list[int]
) -> dict:

    tn, fp, fn, tp = confusion_matrix(
        y_true,
        y_pred
    ).ravel()

    sensitivity = tp / (tp + fn)

    specificity = tn / (tn + fp)

    return {
        "accuracy": accuracy_score(y_true, y_pred),
        "precision": precision_score(y_true, y_pred),
        "f1": f1_score(y_true, y_pred),
        "sensitivity": sensitivity,
        "specificity": specificity
    }
```

---

## Métricas Multiclasse

```python
def calculate_multiclass_metrics(
    self,
    y_true: list[int],
    y_pred: list[int]
) -> dict:

    cm = confusion_matrix(
        y_true,
        y_pred
    )

    sensitivities = []
    specificities = []

    total = cm.sum()

    for i in range(len(cm)):

        tp = cm[i, i]

        fn = cm[i, :].sum() - tp

        fp = cm[:, i].sum() - tp

        tn = total - tp - fp - fn

        sensitivities.append(
            tp / (tp + fn)
        )

        specificities.append(
            tn / (tn + fp)
        )

    return {
        "confusion_matrix": cm,
        "mean_sensitivity": float(np.mean(sensitivities)),
        "mean_specificity": float(np.mean(specificities))
    }
```

---

## Avaliação Final

Depois do treino:

```python
def evaluate(self) -> dict:

    y_true, y_pred = self.validate()

    if self.config.binary_classification:

        return self.calculate_binary_metrics(
            y_true,
            y_pred
        )

    return self.calculate_multiclass_metrics(
        y_true,
        y_pred
    )
```

---

## Histórico para Matplotlib

Você já terá:

```python
self.loss_history
self.accuracy_history
```

que podem ser usados diretamente.

```python
plt.plot(training_manager.loss_history)
```

```python
plt.plot(training_manager.accuracy_history)
```

Ou atualizados em tempo real dentro dos canvases do Tkinter a cada época.

---

No fim, para seu projeto eu teria somente:

```text
DatasetManager
ImageManager
SegmentationProcessor
DataAugmentationProcessor
MammographyDataset
TrainingManager
GradCAMProcessor
GUI
```

Isso fica modular, mas sem excesso de abstrações. O próximo passo depois do `TrainingManager` seria implementar de fato o método `_create_model()` com ResNet18 e EfficientNet-B0 e conectar o botão "Treinar" da GUI.
